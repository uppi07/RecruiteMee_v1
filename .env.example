# Backend runtime
NODE_ENV=production
JWT_SECRET=change-me

# MongoDB Atlas
# Replace <db_password> with your real MongoDB Atlas password
MONGO_URI=mongodb+srv://uppiupendra077_db_user:<db_password>@cluster0.67yblz2.mongodb.net/recruitemee?retryWrites=true&w=majority&appName=Cluster0
MONGO_DNS_SERVERS=1.1.1.1,8.8.8.8
MONGO_URI_FALLBACK=mongodb://127.0.0.1:27017/recruitemee
MONGO_SERVER_SELECTION_TIMEOUT_MS=10000

# Frontend/backend URLs
APP_URL=http://localhost:8080
CLIENT_ORIGINS=http://localhost:8080
VITE_API_URL=http://localhost:5000
BACKEND_HOST_PORT=5000

# Optional backend flag
USE_GRIDFS=0

# Optional default login bootstrap
DEFAULT_LOGIN_ENABLED=1
DEFAULT_LOGIN_USERNAME=1
DEFAULT_LOGIN_PASSWORD=0
DEFAULT_LOGIN_EMAIL=default.user@recruitemee.local
DEFAULT_LOGIN_NAME=Default User

# Email (for forgot-password links)
# Supports SMTP_* (preferred) and EMAIL_* aliases in code.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=0
SMTP_USER=your-email@example.com
SMTP_PASS=your-app-password
MAIL_FROM=your-email@example.com
